# Agent Rules

## Rigorous Testing and Verification
When working on features (especially UI changes or backend integrations), you MUST test that the feature actually works and renders correctly before summarizing your work to the user. Do not assume the code works just because it compiled. If the feature is broken, retry and fix it before reporting success.
